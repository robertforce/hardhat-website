---
title: Using a Hardhat plugin
description: Hardhat 3 Tutorial - Using a Hardhat plugin
sidebar:
  order: 6
---

import Install from "@hh/Install.astro";
import Run from "@hh/Run.astro";

So far, you've been using Hardhat's built-in features. Now let's extend Hardhat's functionality with plugins.

We'll install the `hardhat-toolbox-viem` plugin, which adds many features you'll need for the rest of the tutorial.

Run this command to install with other dependencies it interacts with:

<Install packages="@nomicfoundation/hardhat-toolbox-viem @nomicfoundation/hardhat-ignition viem" />

Import the plugin in your `hardhat.config.ts` file and add it to the plugins list:

```ts ins={2,6}
// hardhat.config.ts
import hardhatToolboxViemPlugin from "@nomicfoundation/hardhat-toolbox-viem";
import { defineConfig } from "hardhat/config";

export default defineConfig({
  plugins: [hardhatToolboxViemPlugin],
  solidity: {
    version: "0.8.28",
  },
});
```

With the plugin installed, run the help command to verify it was loaded:

<Run command="hardhat -h" />

You should see new tasks added by the plugin, like `test nodejs`.

:::tip
Check our [official](/docs/plugins/official-plugins) and [community](/docs/plugins/community-plugins) plugins lists to discover more plugins.
:::
