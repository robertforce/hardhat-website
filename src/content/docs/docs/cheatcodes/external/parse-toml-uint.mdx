---
title: parseTomlUint
description: parseTomlUint cheatcode documentation
---

### Signature

```solidity
function parseTomlUint(
  string calldata toml,
  string calldata key
) external pure returns (uint256);
```

### Description

Parses a string of TOML data at `key` and coerces it to `uint256`.
